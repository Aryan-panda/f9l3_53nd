[pytest]
minversion = 7.0
addopts = -ra -q --strict-markers
testpaths = tests
pythonpath = src
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    security: Security-focused tests
    adversarial: Adversarial simulation tests
